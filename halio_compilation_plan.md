# Legacy Compatibility Note

This file name contains the original typo used during the initial ShuffleNet-only planning pass.

The canonical multi-model Hailo plan now lives in:
- `hailo_compilation_plan.md`

That new document supersedes this file and expands scope from one ShuffleNet checkpoint to all 6 currently trained deployable `RFBinaryDetect` checkpoints.
